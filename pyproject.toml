[tool.poetry]
name = "ollama_framework"
version = "0.1.0"
description = "Shared Ollama integration framework"

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
ollama = ">=0.5.1,<0.6.0"

[build-system]
requires = ["poetry-core>=2.1.3"]
build-backend = "poetry.core.masonry.api"
